
/// The state of the session.
public enum FDASessionState {

    /// The session is recording.
    case recording

    /// The session is playing.
    case playing

    /// The session is paused.
    case pause
}
